# pas.toml — Pascal's Discrete Attractor manifest
# Generated by `pas init`

[project]
name    = "my-project"
version = "0.1.0"

# no toolchains detected — add [toolchain] manually if needed

[quality]
stages = []
# Add your quality check commands here, e.g.:
# stages = ["cargo test", "cargo clippy"]
