app = "ai-gateway-proxy"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile.proxy"
  context = "."

[env]
  GOVERNANCE_URL = "http://ai-gateway-governance.internal:8000"
  LOG_LEVEL = "info"
  DOCS_ENABLED = "false"

[metrics]
  port = 9091
  path = "/metrics"

[[services]]
  internal_port = 8000
  protocol = "tcp"
  auto_stop_machines = true

  [[services.ports]]
    port = 443
    handlers = ["tls", "http"]

  [[services.ports]]
    port = 80
    handlers = ["http"]
    force_https = true

  [services.concurrency]
    type = "connections"
    hard_limit = 250
    soft_limit = 200

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
