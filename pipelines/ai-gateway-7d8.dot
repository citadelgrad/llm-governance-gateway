digraph EpicRunner {
    label="Process all tasks in an epic"
    goal="Implement all child tasks of epic ai-gateway-7d8: Harden auth-architecture.md: adversarial review remediation. Three independent adversarial passes (identity/delegation, systems-consistency, policy/compliance) against docs/auth-architecture.md found contradictions between diagrams, prose, and existing code that would ship governance gaps if built as-is. This epic tracks remediation of those findings before POC build-out. Source: adversarial review conducted 2026-07-28 covering the container diagram, runtime-flow and delegation sequence diagrams, and design-decisions sections."
    model="sonnet"

    start [shape="Mdiamond"]
    done  [shape="Msquare"]

    // --- Pick the next task ---
    pick_task [
        shape="diamond"
        label="Pick Next Task"
        node_type="conditional"
        llm_model="haiku"
        allowed_tools="Bash(bd:*)"
        prompt="Run: bd show ai-gateway-7d8
Look at the BLOCKS section to see child tasks.

Then run: bd ready
Find the first ready (unblocked, open) child task of epic ai-gateway-7d8.

If you find one:
- Run: bd update <task-id> --status=in_progress
- Write the task ID and full description to .pas/current_task.md
- Respond with MORE on the last line

If all child tasks are closed (no more ready tasks for this epic):
- Respond with DONE on the last line"
    ]

    // --- Investigate ---
    investigate [
        shape="box"
        label="Investigate"
        allowed_tools="Read,Grep,Glob,Bash(bd:*)"
        prompt="Read .pas/current_task.md to get the current task ID and description.
Run bd show <task-id> to get full details.

Investigate the codebase to understand what needs to change:
- Find the relevant files and functions
- Understand the current behavior
- Identify what needs to change

Write your findings to .pas/investigation.md including:
- Files to modify
- Current behavior
- Required changes
- Any risks or dependencies"
    ]

    // --- Implement ---
    implement [
        shape="box"
        label="Implement"
        prompt="Read .pas/current_task.md for the task and .pas/investigation.md for the plan.
Implement the changes described. Follow existing code patterns and conventions.
Keep changes minimal and focused on the task requirements."
    ]

    // --- Test ---
    run_tests [
        shape="box"
        label="Run Tests"
        prompt="Read .pas/current_task.md to understand what was changed.
Run the relevant test suite for the modified code.
If tests fail, fix the issues and re-run until green.
Write the test results to .pas/test-results.md"
    ]

    // --- Verify ---
    verify [
        shape="diamond"
        label="Verify"
        node_type="conditional"
        prompt="Read .pas/current_task.md for the task requirements.
Check the changes:
1. Review the git diff
2. Run the linter on changed files
3. Verify tests pass (read .pas/test-results.md)
4. Check that the task's acceptance criteria are met

If everything looks good, respond with PASS on the last line.
If there are problems, respond with FAIL on the last line."
    ]

    fixup [
        shape="box"
        label="Fix Issues"
        prompt="Read .pas/current_task.md and .pas/investigation.md for context.
The verification step found problems. Fix any failing tests, lint errors, or unmet acceptance criteria.
Re-run tests after fixing."
    ]

    // --- Close this task ---
    close_task [
        shape="diamond"
        label="Close Task"
        node_type="conditional"
        allowed_tools="Bash(bd:*),Bash(git:*)"
        prompt="Read .pas/current_task.md to get the task ID.
1. Stage changes: git add -A
2. Commit with a descriptive message referencing the task ID
3. Close the task: bd close <task-id> --reason='Implemented successfully'

If all steps succeeded, respond with SUCCESS on the last line.
If any step failed, respond with FAIL on the last line."
    ]

    // --- Retry a failed close, once ---
    close_task_retry [
        shape="diamond"
        label="Retry Close Task"
        node_type="conditional"
        allowed_tools="Bash(bd:*),Bash(git:*)"
        prompt="The previous attempt to close this task failed. Read .pas/current_task.md to get the task ID.
Retry, skipping any step already done:
1. Check for uncommitted changes and stage them: git add -A
2. Commit with a descriptive message referencing the task ID, if not already committed
3. Close the task: bd close <task-id> --reason='Implemented successfully', if not already closed

If the task is now committed and closed, respond with SUCCESS on the last line.
If it still fails, respond with FAIL on the last line."
    ]

    // --- Give up after one retry; flag for a human instead of silently advancing ---
    flag_for_human [
        shape="box"
        label="Flag For Human"
        allowed_tools="Bash(bd:*)"
        prompt="Read .pas/current_task.md to get the task ID.
Run: bd human <task-id> --reason='close_task failed twice in a row; commit/close needs manual review'
This task's work may be implemented and verified but left uncommitted or unclosed. Do not attempt further automated fixes."
    ]

    halted [shape="Msquare", label="Halted: Needs Human"]

    // --- Check if more tasks remain ---
    check_remaining [
        shape="diamond"
        label="More Tasks?"
        node_type="conditional"
        llm_model="haiku"
        allowed_tools="Bash(bd:*)"
        prompt="Run: bd show ai-gateway-7d8
Look at the BLOCKS section. Count how many child tasks are still open.

Run: bd ready
Check if any remaining child tasks of this epic are ready (unblocked, open).

If there are more ready tasks, respond with MORE on the last line.
If all tasks are closed or remaining tasks are blocked, respond with DONE on the last line."
    ]

    // --- Edges ---
    start -> pick_task

    pick_task -> investigate [label="MORE", condition="preferred_label=MORE"]
    pick_task -> done        [label="DONE", condition="preferred_label=DONE"]

    investigate -> implement -> run_tests -> verify

    verify -> close_task [label="PASS", condition="preferred_label=PASS"]
    verify -> fixup      [label="FAIL", condition="preferred_label=FAIL"]
    fixup  -> verify

    close_task -> check_remaining  [label="SUCCESS", condition="preferred_label=SUCCESS"]
    close_task -> close_task_retry [label="FAIL", condition="preferred_label=FAIL"]

    close_task_retry -> check_remaining [label="SUCCESS", condition="preferred_label=SUCCESS"]
    close_task_retry -> flag_for_human  [label="FAIL", condition="preferred_label=FAIL"]

    flag_for_human -> halted

    check_remaining -> pick_task [label="MORE", condition="preferred_label=MORE", loop_restart=true]
    check_remaining -> done      [label="DONE", condition="preferred_label=DONE"]
}
