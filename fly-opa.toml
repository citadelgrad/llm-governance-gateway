app = "ai-gateway-opa"
primary_region = "iad"

[build]
  image = "openpolicyagent/opa:latest-rootless"

# No [[services]] block — internal network only via .internal DNS
# Reachable at http://ai-gateway-opa.internal:8181 from other Fly apps

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
