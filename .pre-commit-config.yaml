repos:
  - repo: local
    hooks:
      - id: security-check
        name: Security — block secrets
        entry: scripts/pre-commit-security.sh
        language: script
        pass_filenames: false
        always_run: true
